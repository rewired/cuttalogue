fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.12
httpx>=0.27
truststore>=0.9
mcp>=2,<3

# Phase 3a: local lyrics-to-vocal forced alignment (see app/alignment.py).
# CPU-only wheels install by default on Windows/pip; a CUDA-enabled torch
# build is used automatically instead if one is already installed - never
# required. First alignment run downloads the ~1GB MMS forced-alignment
# model into torch's normal hub cache (~/.cache/torch/hub/checkpoints/).
torch>=2.5
torchaudio>=2.5
